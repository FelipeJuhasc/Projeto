class curso {
    constructor (){
        this._id = id;id, DTiniIncs, DTfimInsc, CargaH, CargaHTCC, Codiesde, Gradeiesde, CursoInst, IniImediato, IniMat, FimMat, Matcoord, PasSeg
        this._DTiniIncs = DTiniIncs;
        this._DTfimInsc = DTfimInsc;
        this._CargaH = CargaH;
        this._CargaHTCC = CargaHTCC;
        this._Codiesde = Codiesde;
        this._Gradeiesde = Gradeiesde;
        this._CursoInst = CursoInst;
        this._IniImediato = IniImediato;
        this._IniMat = IniMat;
        this._FimMat = FimMat;
        this._Matcoord = Matcoord;
        this._PasSeg = PasSeg;
    }

  get id() { return this._id; }
  set id(valor) { this._id = valor; }

  get DTiniIncs() { return this._DTiniIncs; }
  set DTiniIncs(valor) { this._DTiniIncs = valor; }

  get DTfimInsc() { return this._DTfimInsc; }
  set DTfimInsc(valor) { this._DTfimInsc = valor; }

  get CargaH() { return this._CargaH; }
  set CargaH(valor) { this._CargaH = valor; }

  get CargaHTCC() { return this._CargaHTCC; }
  set CargaHTCC(valor) { this._CargaHTCC = valor; }

  get Codiesde() { return this._Codiesde; }
  set Codiesde(valor) { this._Codiesde = valor; }

  get Gradeiesde() { return this._Gradeiesde; }
  set Gradeiesde(valor) { this._Gradeiesde = valor; }

  get CursoInst() { return this._CursoInst; }
  set CursoInst(valor) { this._CursoInst = valor; }

  get IniImediato() { return this._IniImediato; }
  set IniImediato(valor) { this._IniImediato = valor; }

  get IniMat() { return this._IniMat; }
  set IniMat(valor) { this._IniMat = valor; }

  get FimMat() { return this._FimMat; }
  set FimMat(valor) { this._FimMat = valor; }

  get Matcoord() { return this._Matcoord; }
  set Matcoord(valor) { this._Matcoord = valor; }
  
  get PasSeg() { return this._PasSeg; }
  set PasSeg(valor) { this._PasSeg = valor; }

    
}